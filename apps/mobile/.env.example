# Convex deployment URL — the SAME backend the web app uses.
# Find it in packages/backend (run `yarn workspace @remember/backend dev`,
# which prints the URL) or in your Convex dashboard.
# Expo only exposes variables prefixed with EXPO_PUBLIC_ to the client.
EXPO_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
